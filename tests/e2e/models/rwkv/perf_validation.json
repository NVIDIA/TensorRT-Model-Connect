{
  "models": [
    {
      "model": "RWKV/rwkv-4-169m-pile",
      "pipeline_type": "rwkv_recurrent",
      "label": "A6-rwkv",
      "benchmark": {
        "label": "CPU argmax",
        "gpu_argmax_label": "GPU argmax",
        "metric": "tok/s",
        "command": ["{binary}", "run", "{bundle}", "--prompt", "{prompt}", "--max-new-tokens", "{max_tokens}", "{config_args}"]
      }
    }
  ]
}
