{
  "models": [
    {
      "model": "suno/bark-small",
      "pipeline_type": "text_to_audio_bark",
      "label": "E1-bark",
      "benchmark": {
        "label": "generate-audio",
        "gpu_argmax_label": "generate-audio",
        "metric": "pipeline_ms",
        "command": ["{binary}", "generate-audio", "{bundle}", "--prompt", "{prompt}", "--output", "{generated_output_wav}", "--max-new-tokens", "{max_tokens}"]
      }
    }
  ]
}
