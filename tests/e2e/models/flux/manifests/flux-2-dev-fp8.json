{
  "name": "flux-2-dev-fp8",
  "trace_id": "IT-E2E-FLX2D-FP8-01",
  "hf_id": "black-forest-labs/FLUX.2-dev",
  "bundle": "flux-2-dev-fp8.trtfb",
  "model_type": "flux.2",
  "family": "flux",
  "runtime_strategy": "diffusion_flux",
  "task_strategy": "diffusion_media_generation",
  "gated": true,
  "ci_tier": "nightly_only",
  "precision": "fp16",
  "reference_family": "diffusers_image_gen",
  "user_contract": "diffusion_image",
  "l0_replacement": "flux-2-dev-fp8-l0",
  "l0_replacement_reason": "FLUX.2-dev FP8 scale stays nightly; flux-2-dev-fp8-l0 keeps the FP8 Flux2 image path at 384px/20 steps for PR L0.",
  "description": "FLUX.2-dev FP8 text-to-image (pre-computed scales, baked x_embedder+ctx_embedder+temb)",
  "build_args": {
    "max_cache_length": 256
  },
  "fp8_scales": "data/flux2-fp8-scales.json",
  "test_prompt": "A photo of a cat sitting on a windowsill at sunset",
  "test_type": "diffusion",
  "image_height": 1024,
  "image_width": 1024,
  "video_num_frames": 1,
  "num_inference_steps": 28,
  "stages": [
    {
      "name": "end_to_end",
      "required": true
    }
  ],
  "skip_text_comparison": true,
  "notes": "FLUX.2-dev FP8: uses pre-computed scales (data/flux2-fp8-scales.json) to skip calibration. All preprocessor ops baked into TRT DiT engine. The human contract is the end-to-end TRT image artifact against the HF Diffusers reference image plus the VLM assessment. The generic diffusion debug substages are not declared here because the current debug pipeline is Wan-specific and is not a FLUX.2 reference oracle.",
  "metadata": {
    "contract_config": {
      "use_diffusers": true
    }
  }
}
