// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
// @title: Benchmark timing scopes
// @description: Wall-clock command time covers the full process, per-token decode time covers the steady-state token loop, and raw enqueue time isolates TensorRT execution, so the three metrics must not be compared as equivalents.
digraph BenchmarkTimingScopes {
  graph [
    bgcolor="#fbfcfe",
    pad="0.35",
    nodesep="0.48",
    ranksep="0.58",
    rankdir="TB",
    splines="polyline",
    fontname="Arial",
    labelloc="t",
    label=<<B>Three different timing boundaries</B><BR/><FONT POINT-SIZE="16">Name the boundary before comparing or reporting a latency number.</FONT>>,
    fontsize="28",
    fontcolor="#111827"
  ];
  node [
    shape="rect",
    style="rounded,filled",
    fillcolor="#ffffff",
    color="#64748b",
    penwidth="2",
    fontname="Arial",
    fontsize="17",
    fontcolor="#111827",
    margin="0.20,0.15"
  ];
  edge [color="#64748b", penwidth="2.4", arrowsize="0.8"];

  wall [
    fillcolor="#f5f3ff",
    color="#7c3aed",
    label=<<FONT POINT-SIZE="20"><B>Wall-clock CLI time</B></FONT>>
  ];
  wall_scope [
    fillcolor="#f5f3ff",
    color="#7c3aed",
    label=<<B>Whole command</B><BR/><FONT POINT-SIZE="16">parse · load · tokenize · host · engine · postprocess</FONT>>
  ];
  decode [
    fillcolor="#ecfccb",
    color="#4d7c0f",
    label=<<FONT POINT-SIZE="20"><B>Per-token decode time</B></FONT>>
  ];
  decode_scope [
    fillcolor="#ecfccb",
    color="#4d7c0f",
    label=<<B>Steady-state token loop</B><BR/><FONT POINT-SIZE="16">provider-reported decode phase</FONT>>
  ];
  enqueue [
    fillcolor="#eff6ff",
    color="#2563eb",
    label=<<FONT POINT-SIZE="20"><B>Raw engine enqueue time</B></FONT>>
  ];
  enqueue_scope [
    fillcolor="#eff6ff",
    color="#2563eb",
    label=<<B>TensorRT execution only</B><BR/><FONT POINT-SIZE="16">excludes surrounding pipeline work</FONT>>
  ];

  { rank=same; wall; wall_scope; }
  { rank=same; decode; decode_scope; }
  { rank=same; enqueue; enqueue_scope; }
  wall -> wall_scope;
  decode -> decode_scope;
  enqueue -> enqueue_scope;
  wall -> decode [style="invis", weight="10"];
  decode -> enqueue [style="invis", weight="10"];
}
