// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
// @title: Build-time units and ownership
// @description: Public build entry points resolve one model owner and call model.build exactly once; that same folder owns its descriptor, Python construction, native runtime, tests, and complete deployment bundle.
digraph BuildOwnership {
  graph [
    bgcolor="#fbfcfe",
    pad="0.35",
    nodesep="0.55",
    ranksep="0.65",
    rankdir="TB",
    splines="polyline",
    fontname="Arial",
    labelloc="t",
    label=<<B>One model owner, one vertical implementation</B><BR/><FONT POINT-SIZE="16">Shared entry points resolve and dispatch; one folder owns build, runtime, tests, and bundle.</FONT>>,
    fontsize="28",
    fontcolor="#111827"
  ];
  node [
    shape="rect",
    style="rounded,filled",
    fillcolor="#ffffff",
    color="#64748b",
    penwidth="2",
    fontname="Arial",
    fontsize="17",
    fontcolor="#111827",
    margin="0.20,0.15"
  ];
  edge [
    color="#64748b",
    penwidth="2.4",
    arrowsize="0.8",
    fontname="Arial",
    fontsize="16",
    fontcolor="#334155"
  ];

  subgraph cluster_public {
    label=<<B>Public build surfaces</B>>;
    color="#7c3aed";
    fillcolor="#f5f3ff";
    style="rounded,filled";
    penwidth="2";
    fontsize="18";
    fontcolor="#4c1d95";
    cli [label=<<B>trtmc build</B>>];
    python [label=<<B>Python build()</B>>];
  }

  subgraph cluster_family {
    label=<<B>models/&lt;owner&gt;/</B>>;
    color="#2563eb";
    fillcolor="#eff6ff";
    style="rounded,filled";
    penwidth="2";
    fontsize="18";
    fontcolor="#1e3a8a";
    descriptor [label=<<B>Single MODEL.toml</B><BR/><FONT POINT-SIZE="16">identity · runtime · manifests</FONT>>];
    model [label=<<B>model.py</B><BR/><FONT POINT-SIZE="16">match · config · weights · route · bundle</FONT>>];
    native [label=<<B>runtime/</B><BR/><FONT POINT-SIZE="16">native recipe + model DSO sources</FONT>>];
    optimized [label=<<B>Exact optimized profile</B><BR/><FONT POINT-SIZE="16">owner-local implementation + adapter</FONT>>];
    evidence [label=<<B>tests/</B><BR/><FONT POINT-SIZE="16">unit · C++ · manifests · E2E evidence</FONT>>];
  }

  bundle [
    fillcolor="#fffbeb",
    color="#b45309",
    label=<<FONT POINT-SIZE="20"><B>.bundle deployment contract</B></FONT><BR/><FONT POINT-SIZE="16">metadata · plans or embedded implementation · assets</FONT>>
  ];
  { rank=same; cli; python; }
  { rank=same; native; optimized; }

  cli -> descriptor;
  python -> descriptor;
  descriptor -> model;
  model -> native;
  model -> optimized;
  native -> bundle;
  optimized -> bundle;
  evidence -> model [style="dashed", label="proves owner contract"];
  evidence -> optimized [style="dashed", label="proves exact profile"];
}
