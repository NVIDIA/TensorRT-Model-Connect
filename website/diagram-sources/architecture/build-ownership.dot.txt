// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
// @title: Build-time units and ownership
// @description: Public build entry points resolve one family model module and call model.build exactly once; that module owns native or exact optimized construction and complete deployment-bundle assembly.
digraph BuildOwnership {
  graph [
    bgcolor="#fbfcfe",
    pad="0.35",
    nodesep="0.55",
    ranksep="0.65",
    rankdir="TB",
    splines="polyline",
    fontname="Arial",
    labelloc="t",
    label=<<B>Build-time ownership</B><BR/><FONT POINT-SIZE="16">Shared entry points resolve and dispatch; one family model module owns the complete bundle.</FONT>>,
    fontsize="28",
    fontcolor="#111827"
  ];
  node [
    shape="rect",
    style="rounded,filled",
    fillcolor="#ffffff",
    color="#64748b",
    penwidth="2",
    fontname="Arial",
    fontsize="17",
    fontcolor="#111827",
    margin="0.20,0.15"
  ];
  edge [
    color="#64748b",
    penwidth="2.4",
    arrowsize="0.8",
    fontname="Arial",
    fontsize="16",
    fontcolor="#334155"
  ];

  subgraph cluster_public {
    label=<<B>Public build surfaces</B>>;
    color="#7c3aed";
    fillcolor="#f5f3ff";
    style="rounded,filled";
    penwidth="2";
    fontsize="18";
    fontcolor="#4c1d95";
    cli [label=<<B>trtmc build</B>>];
    python [label=<<B>Python build()</B>>];
  }

  subgraph cluster_family {
    label=<<B>Model-owned build units</B>>;
    color="#2563eb";
    fillcolor="#eff6ff";
    style="rounded,filled";
    penwidth="2";
    fontsize="18";
    fontcolor="#1e3a8a";
    descriptor [label=<<B>Family descriptor</B><BR/><FONT POINT-SIZE="16">MODEL.toml + match rules</FONT>>];
    model [label=<<B>Family model.py</B><BR/><FONT POINT-SIZE="16">match · config · weights · route · bundle</FONT>>];
    native [label=<<B>Native recipe</B><BR/><FONT POINT-SIZE="16">graph + TensorRT plans + sections</FONT>>];
    optimized [label=<<B>Exact optimized profile</B><BR/><FONT POINT-SIZE="16">implementation + isolated adapter</FONT>>];
  }

  bundle [
    fillcolor="#fffbeb",
    color="#b45309",
    label=<<FONT POINT-SIZE="20"><B>.bundle deployment contract</B></FONT><BR/><FONT POINT-SIZE="16">metadata · plans or embedded implementation · assets</FONT>>
  ];
  evidence [
    fillcolor="#ecfeff",
    color="#0e7490",
    label=<<B>Owned evidence</B><BR/><FONT POINT-SIZE="16">unit · E2E · exact-profile qualification</FONT>>
  ];

  { rank=same; cli; python; }
  { rank=same; native; optimized; }

  cli -> descriptor;
  python -> descriptor;
  descriptor -> model;
  model -> native;
  model -> optimized;
  native -> bundle;
  optimized -> bundle;
  evidence -> model [style="dashed", label="proves family contract"];
  evidence -> optimized [style="dashed", label="proves exact profile"];
}
