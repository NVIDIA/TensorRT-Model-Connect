// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
// @title: Native and optimized build-route selection
// @description: The shared builder resolves one family and calls its model.build exactly once; that family module owns any exact optimized-profile decision, its native recipe, bundle assembly, and fail-closed errors.
digraph BuildRouteSelection {
  graph [
    bgcolor="#fbfcfe",
    pad="0.35",
    nodesep="0.50",
    ranksep="0.72",
    rankdir="TB",
    splines="polyline",
    fontname="Arial",
    labelloc="t",
    label=<<B>Select one build route</B><BR/><FONT POINT-SIZE="16">Shared code resolves and dispatches once; the family model owns every later decision.</FONT>>,
    fontsize="28",
    fontcolor="#111827"
  ];
  node [
    shape="rect",
    style="rounded,filled",
    fillcolor="#ffffff",
    color="#64748b",
    penwidth="2",
    fontname="Arial",
    fontsize="17",
    fontcolor="#111827",
    margin="0.20,0.15"
  ];
  edge [
    color="#64748b",
    penwidth="2.4",
    arrowsize="0.8",
    fontname="Arial",
    fontsize="16",
    fontcolor="#334155"
  ];

  request [
    fillcolor="#f5f3ff",
    color="#7c3aed",
    label=<<B>Source model</B><BR/><FONT POINT-SIZE="16">checkpoint config + revision</FONT>>
  ];
  family [
    fillcolor="#eff6ff",
    color="#2563eb",
    label=<<B>Resolve owning family</B><BR/><FONT POINT-SIZE="16">descriptor metadata bounds candidates</FONT>>
  ];
  model [
    fillcolor="#eff6ff",
    color="#2563eb",
    label=<<B>Import family model.py</B><BR/><FONT POINT-SIZE="16">call model.build() exactly once</FONT>>
  ];
  probe_inputs [
    fillcolor="#f5f3ff",
    color="#7c3aed",
    label=<<B>Family-owned route inputs</B><BR/><FONT POINT-SIZE="16">revision + target + effective options</FONT>>
  ];
  outcome [
    shape="diamond",
    fillcolor="#fffbeb",
    color="#b45309",
    label=<<B>Family selects one exact<BR/>optimized profile?</B>>
  ];
  native [
    fillcolor="#ecfccb",
    color="#4d7c0f",
    label=<<B>Family native recipe</B><BR/><FONT POINT-SIZE="16">config → weights → plans → sections</FONT>>
  ];
  optimized [
    fillcolor="#f5f3ff",
    color="#7c3aed",
    label=<<B>Isolated optimized adapter</B><BR/><FONT POINT-SIZE="16">package exact implementation tree</FONT>>
  ];
  native_bundle [
    fillcolor="#fffbeb",
    color="#b45309",
    label=<<B>Native .bundle</B><BR/><FONT POINT-SIZE="16">runtime_strategy + plans + assets</FONT>>
  ];
  optimized_bundle [
    fillcolor="#fffbeb",
    color="#b45309",
    label=<<B>Optimized .bundle</B><BR/><FONT POINT-SIZE="16">descriptor + embedded implementation DSO</FONT>>
  ];
  terminal [
    fillcolor="#fef2f2",
    color="#b91c1c",
    label=<<B>Fail closed</B><BR/><FONT POINT-SIZE="16">multiple claims → error</FONT><BR/><FONT POINT-SIZE="16">claimed adapter failure → terminal</FONT>>
  ];

  request -> family -> model -> outcome;
  probe_inputs -> outcome;
  outcome -> optimized [label="yes"];
  outcome -> native [label="no"];
  outcome -> terminal [label="invalid / multiple"];
  native -> native_bundle;
  optimized -> optimized_bundle;
  optimized -> terminal [style="dashed", label="build failure"];

  { rank=same; native; outcome; }
  { rank=same; native_bundle; optimized_bundle; }
  { rank=same; optimized_bundle; terminal; native_bundle; }
}
